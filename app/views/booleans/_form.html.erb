<%= form_with(model: boolean) do |form| %>
  <% if boolean.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(boolean.errors.count, "error") %> prohibited this boolean from being saved:</h2>

      <ul>
        <% boolean.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :fixed, style: "display: block" %>
    <%= form.check_box :fixed %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
