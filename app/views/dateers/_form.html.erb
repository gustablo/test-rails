<%= form_with(model: dateer) do |form| %>
  <% if dateer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(dateer.errors.count, "error") %> prohibited this dateer from being saved:</h2>

      <ul>
        <% dateer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :data, style: "display: block" %>
    <%= form.date_field :data %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
